version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: dev-proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - devnet

networks:
  devnet:
    external: true
